/**
 * @author Martín Luz
 * @version 1.0.0
 * @description Product Detail Page Controller
 */

const ProductDetailController = (req, res) => {
  res.send(`Hello im the product detail page`);
};

module.exports = ProductDetailController;